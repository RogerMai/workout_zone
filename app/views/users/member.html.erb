<h1>Workout Zone Member</h1>
<table>
    <tr>
        <th>Username</th>
        <th>Created At</th>
    </tr>
    <% @users.each do |user| %>
        <tr>
            <td><%= user.username %></td>
            <td><%= user.created_at %></td>
            <td><%= link_to "Show", user %></td>
            <td><%= link_to "Edit", edit_user_path(user) %></td>
        </tr>    
    <% end %>
</table>
<%= link_to "New User", users_new_path %>